from setuptools import setup

setup(name='simplefriendlybank',
      version='1.0.0',
      description='A sample Python project', 
      python_requires='>=3',
      packages = ['simplefriendlybank']
)